import buttons from './buttons';
import commands from './commands';
import events from './events';
import modals from './modals';
import selects from './selects';

export default { events, commands, buttons, selects, modals };